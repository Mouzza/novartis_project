@model JPP.BL.Domain.Modules.DossierModule

@{
    ViewBag.title = "Dossier antwoorden";

}

@if (User.IsInRole("Admin") || User.IsInRole("Moderator"))
{

<div id="Lijst">

    @{Html.RenderAction("Lijst", "Antwoord", new { id = Model.ID });}
</div>

<div id="Lijst2">

    @{Html.RenderAction("Lijst2", "Antwoord", new { id = Model.ID });}
</div>
}