@model JPP.UI.Web.MVC.Models.User
@{
    
        
}


@if (User.IsInRole("Admin") || User.IsInRole("Moderator"))
{
    if(User.IsInRole("Admin")) {
    <h1 id="paginaTitel">Admin paneel</h1>
        ViewBag.title = "Admin paneel";
    }
    else
    {
        <h1 id="paginaTitel">Moderator paneel</h1> 
        ViewBag.title = "Moderator paneel";
    }
    <div class="row">
        <div class="col-md-4 centraal">

            <a href="@Url.Action("Index", "Gebruiker")"><img src="~/Images/menu logos/gebruikersLogo.png"></a>
            <h2>Gebruikers</h2>
        </div>
        @if(User.IsInRole("Admin")) {
            
        <div class="col-md-4 centraal">

            <a href="@Url.Action("Index", "Roles")"><img src="~/Images/menu logos/rolesLogo.png"></a>
            <h2>Roles</h2>
        </div>
        }
     
        <div class="col-md-4 centraal">
            <a href="@Url.Action("Index", "Module")"><img src="~/Images/menu logos/moduleLogo.png"></a>
            <h2>Modules</h2>
        </div>

    </div>

  

        }
